/*!
 * Bootstrap v5.3.3 Lightweight Bundle JS
 * Local embed for Podcast & Media Channel Researcher & AI Analyzer (GPL-3.0)
 */
(function() {
  'use strict';

  // Tab Handler
  document.addEventListener('click', function(e) {
    var tabTrigger = e.target.closest('[data-bs-toggle="tab"], [data-bs-toggle="pill"]');
    if (!tabTrigger) return;
    e.preventDefault();

    var targetSelector = tabTrigger.getAttribute('data-bs-target') || tabTrigger.getAttribute('href');
    if (!targetSelector) return;

    var nav = tabTrigger.closest('.nav, .nav-tabs, .nav-pills');
    if (nav) {
      nav.querySelectorAll('.nav-link').forEach(function(link) {
        link.classList.remove('active');
        link.setAttribute('aria-selected', 'false');
      });
    }
    tabTrigger.classList.add('active');
    tabTrigger.setAttribute('aria-selected', 'true');

    var targetPane = document.querySelector(targetSelector);
    if (targetPane) {
      var tabContent = targetPane.closest('.tab-content');
      if (tabContent) {
        tabContent.querySelectorAll('.tab-pane').forEach(function(pane) {
          pane.classList.remove('active', 'show');
        });
      }
      targetPane.classList.add('active', 'show');
    }
  });

  // Offcanvas & Modal Dismiss Handler
  document.addEventListener('click', function(e) {
    var dismissTrigger = e.target.closest('[data-bs-dismiss="offcanvas"], [data-bs-dismiss="modal"]');
    if (!dismissTrigger) return;
    e.preventDefault();

    var container = dismissTrigger.closest('.offcanvas, .modal-backdrop-custom, .custom-drawer');
    if (container) {
      container.classList.remove('show');
      container.style.display = 'none';
      var backdrop = document.querySelector('.drawer-backdrop');
      if (backdrop) backdrop.style.display = 'none';
    }
  });

})();
